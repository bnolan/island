<p class="breadcrumbs">
  <%= link_to "Items", items_url %> &gt; Edit <%= @item.name %>
</p>

<h1><%= @item.name %></h1>

<div id="item-viewer">
</div>

<ul class="actions">
  <li><%= link_to 'Delete', @item, :method => :delete %></li>
</ul>

<% form_for @item do |f| %>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <%= submit_tag 'SAVE' %> or <%= link_to 'cancel', :back %>

<% end %>

<script>

x = {
  onIdle : function(){
    this.el.effect('bounce');
  },
  
  getVerbs : function(){
    return ["pickup"];
  },
  
  onRemove : function(){
    this.el.fadeOut();
    // new Sound("...");​
  }

};

  $ITEM = <%= @item.to_json.html_safe.gsub(/\u200/,'').gsub('\\b','') %>;
</script>
