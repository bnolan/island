<h1>Assets</h1>

<ul class="actions">
  <li><%= link_to 'New asset', new_asset_url %></li>
</ul>

<div>
  <% Asset.all.each do |asset| %>
    <% if asset.image? %>
      <%= link_to image_tag(asset.upload.url, { 'data-id' => asset.id } ), asset %>
    <% end %>
  <% end %>
</div>
