

<div class="toolbox">
  <% Asset.all.each do |asset| %>
    <% if asset.image? %>
      <%= link_to image_tag(asset.upload.url, { 'data-id' => asset.id } ), asset %>
    <% end %>
  <% end %>
</div>
